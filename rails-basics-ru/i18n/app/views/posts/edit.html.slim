= content_for :title do
  = t('.title')

== render 'form', post: @post

hr

.d-flex
  = link_to t('actions.show'), @post, class: 'btn btn-outline-primary me-2'
  = link_to t('actions.back'), :back, class: 'btn btn-outline-primary'
